## Sprint 2: The Skeleton & Typography (Layout)

**Goal:** Implement the 12-column grid and the "Terminal" theme foundation.

* **In-Scope:** CSS Variables, Grid/Flexbox layout, Font imports (JetBrains Mono/Inter).
* **Out-of-Scope:** Project cards, mobile responsiveness, interactivity.
* **Tasks:**
    1. Import fonts via Google Fonts or `@font-face`.
    2. Set CSS variables for `#121212` (bg) and `#00FFAB` (primary).
    3. Create the fixed desktop sidebar and the main content `<section>` tags.
* **Files:** `style.css`, `index.html`
* **Acceptance Criteria:** A dark screen with a sidebar on the left and a scrolling area on the right.
* **Avoid:** Building a mobile-specific navigation bar yet.
* **Evidence:** A screenshot of the layout with the correct "Deep Charcoal" background.